# ==========================================================================
# Dependências (reinstaláveis com npm install / pip install)
# ==========================================================================
node_modules/

# Ambiente virtual do Python
venv/
.venv/
__pycache__/
*.pyc

# ==========================================================================
# Saída de build (a hospedagem gera sozinha a cada deploy)
# ==========================================================================
dist/
dist-ssr/

# ==========================================================================
# Segredos — NUNCA versionar
# O site é estático e não usa variável de ambiente nenhuma. As regras ficam
# como rede de proteção, para o caso de alguma ser introduzida no futuro.
# ==========================================================================
.env
.env.*

# ==========================================================================
# Logs e temporários
# ==========================================================================
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
*.local

# ==========================================================================
# Configuração do Claude Code (local da máquina, não vai para o repositório)
# ==========================================================================
.claude/

# ==========================================================================
# Editor e sistema operacional
# ==========================================================================
.vscode/*
!.vscode/extensions.json
.idea/
.DS_Store
Thumbs.db
*.suo
*.sw?
